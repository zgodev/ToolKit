<?xml version="1.0" encoding="utf-8"?>
<!--
    默认禁止明文 HTTP；debug 打包时通过 debug-overrides 放开，方便本地联调。
    生产环境接入时若需要额外域名可加 <domain-config> 单独声明。
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>

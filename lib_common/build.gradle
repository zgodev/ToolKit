plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
    id 'org.jetbrains.kotlin.kapt'
    id 'com.google.devtools.ksp'
    id 'dagger.hilt.android.plugin'
}

android {
    namespace 'com.zhangyt.common'
    compileSdk rootProject.ext.android.compileSdkVersion
    buildToolsVersion rootProject.ext.android.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.android.minSdkVersion
        targetSdkVersion rootProject.ext.android.targetSdkVersion
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
    buildFeatures {
        viewBinding true
        dataBinding true
        buildConfig true
    }
    resourcePrefix "common_"   // 资源前缀避免冲突
}

// ARouter：kapt DSL + 全局 JavaCompile 两层兜底（Hilt 的 hiltJavaCompile task 需要后者）
kapt {
    arguments {
        arg("AROUTER_MODULE_NAME", project.name)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.compilerArgs.add("-AAROUTER_MODULE_NAME=${project.name}")
}

dependencies {
    // lib_common 使用 api 依赖，让其他 module 可直接使用
    api rootProject.ext.dependencies["appcompat"]
    api rootProject.ext.dependencies["core-ktx"]
    api rootProject.ext.dependencies["material"]
    api rootProject.ext.dependencies["constraint-layout"]
    api rootProject.ext.dependencies["recyclerview"]
    api rootProject.ext.dependencies["fragment-ktx"]
    api rootProject.ext.dependencies["viewpager2"]
    api rootProject.ext.dependencies["multidex"]
    api rootProject.ext.dependencies["activity-ktx"]

    // Lifecycle
    api rootProject.ext.dependencies["lifecycle-viewmodel"]
    api rootProject.ext.dependencies["lifecycle-livedata"]
    api rootProject.ext.dependencies["lifecycle-runtime"]

    // Coroutines
    api rootProject.ext.dependencies["coroutines-core"]
    api rootProject.ext.dependencies["coroutines-android"]

    // ARouter 路由
    api rootProject.ext.dependencies["arouter-api"]
    kapt rootProject.ext.dependencies["arouter-compiler"]

    // Glide（4.16 起官方 ksp 支持）
    api rootProject.ext.dependencies["glide"]
    ksp rootProject.ext.dependencies["glide-compiler"]

    // 工具库
    api rootProject.ext.dependencies["utilcode"]
    api rootProject.ext.dependencies["mmkv"]

    // 屏幕适配
    api rootProject.ext.dependencies["autosize"]

    // EventBus
    api rootProject.ext.dependencies["eventbus"]

    // RecyclerView 适配器
    api rootProject.ext.dependencies["brvah"]

    api rootProject.ext.dependencies["worker"]
    api rootProject.ext.dependencies["xlog"]

    // Hilt（api 让业务模块直接使用 @Inject / @HiltViewModel）
    api rootProject.ext.dependencies["hilt"]
    ksp rootProject.ext.dependencies["hilt-compiler"]

    // TBS X5 WebView 内核
    api rootProject.ext.dependencies["tbs"]

    // 依赖其他基础模块
    api project(path: ':utils')
    api project(path: ':widget')
    api project(path: ':network')
}
